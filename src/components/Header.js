import React from "react";

import Herobox from "./Herobox";

const Header = () => {
  return (
    <header className="header">
      <Herobox />
    </header>
  );
};

export default Header;
